#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lambda-packager/bin/lambda-packager" "$@"
  ret=$?
else 
  node  "$basedir/../lambda-packager/bin/lambda-packager" "$@"
  ret=$?
fi
exit $ret
